import { createGlobalStyle } from 'styled-components'

export const StoryBookGlobalStyles = createGlobalStyle`
    
    #root {
      width: 100%;
      display: block;
      padding: 0 !important;
    }

`
